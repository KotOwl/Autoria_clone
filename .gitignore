# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Python specific (for backend, PyCharm, etc.)
__pycache__/
*.pyc
*.pyd
*.pyo
venv/
.venv/
.idea/
.env

# Database related files (adjust if you have specific local DB files or dumps)
# db.sqlite3
# *.db
 mysql/

# Frontend-specific ignores (these apply to the 'frontend' subfolder)
# Note: No leading slash for these rules, or explicitly define the path.
# Using 'frontend/' ensures it's specific to that folder.
frontend/node_modules/
frontend/build/
frontend/.pnp/
frontend/.pnp.js
frontend/coverage/

# Misc common ignores (apply to the whole repository)
.DS_Store
# Specific .env files that might be used by frontend within its folder,
# or if they can appear anywhere.
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
mysql/mysql.sock